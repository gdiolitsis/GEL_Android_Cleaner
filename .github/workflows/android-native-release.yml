name: Manual Build — GEL Android Cleaner v4

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --------------------------------------------
      # Install JDK + Android NDK + CMake (NDK-enabled build)
      # --------------------------------------------
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Install Android NDK + CMake
        uses: android-actions/setup-android@v3
        with:
          sdk-tools: true
          ndk-version: "25.2.9519653"
          cmake-version: "3.22.1"

      - name: Install Gradle
        run: |
          wget https://services.gradle.org/distributions/gradle-8.2.1-bin.zip
          unzip gradle-8.2.1-bin.zip
          echo "GRADLE_HOME=$PWD/gradle-8.2.1" >> $GITHUB_ENV
          echo "$PWD/gradle-8.2.1/bin" >> $GITHUB_PATH

      - name: Enable AndroidX
        run: |
          echo "android.useAndroidX=true" >> gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties

      - name: Decode Keystore
        run: |
          echo "$KEYSTORE_B64" | base64 --decode > gel-keystore.jks
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_BASE64 }}

      # --------------------------------------------
      # BUILD RELEASE (NDK + CMake handled by Gradle)
      # --------------------------------------------
      - name: Build Release (with NDK)
        run: gradle assembleRelease

      # --------------------------------------------
      # Sign & Align
      # --------------------------------------------
      - name: Install Android Build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y zipalign apksigner

      - name: Align + Sign APK
        run: |
          cd app/build/outputs/apk/release
          echo ">>> Found APK:"
          ls -1 *.apk
          
          unsigned=$(ls *-unsigned.apk | head -n 1)
          echo ">>> Signing: $unsigned"

          zipalign -f 4 "$unsigned" aligned.apk

          apksigner sign \
            --ks $GITHUB_WORKSPACE/gel-keystore.jks \
            --ks-pass pass:"$KEYSTORE_PASSWORD" \
            --key-pass pass:"$KEY_ALIAS_PASSWORD" \
            --ks-key-alias "$KEY_ALIAS" \
            --out signed.apk \
            aligned.apk

          mv signed.apk GEL_Android_Cleaner_v4.apk
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS_PASSWORD: ${{ secrets.KEY_ALIAS_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}

      # --------------------------------------------
      # Upload final APK
      # --------------------------------------------
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GEL_Android_Cleaner_Release_APK_v4
          path: app/build/outputs/apk/release/GEL_Android_Cleaner_v4.apk
