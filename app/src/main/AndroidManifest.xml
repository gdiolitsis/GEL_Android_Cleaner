<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gel.cleaner"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Επιτρέπεται να τερματίζουμε background διεργασίες άλλων apps -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>

    <!-- Για να ανοίγουμε το Accessibility settings (ο χρήστης το ενεργοποιεί χειροκίνητα) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" xmlns:tools="http://schemas.android.com/tools"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity
            android:name="com.gel.cleaner.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Accessibility Service για auto "Clear cache" (EN/EL) -->
        <service
            android:name="com.gel.cleaner.AccessibilityCleanerService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_cleaner_config"/>
        </service>

    </application>
</manifest>
